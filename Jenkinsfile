pipeline {
    agent any

    stages {
        stage('stage gd3na mny kda') {
            steps {
                sh 'ls' 
            }
        }
        

        stage('BuilD') {
            steps{
                sh 'docker-compose up --build' 
            }
        }
    }
}